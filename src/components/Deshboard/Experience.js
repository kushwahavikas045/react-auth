import React from 'react'
import DataNotFound from '../Layout/DataNotFound';

const Experience = () => {

   //demo experience list of developer
   const experience = [];
   //dynamic content variable
   let content;

   if(experience.length === 0){
       return content = <DataNotFound/>
   }

    return (
        content
    )
}

export default Experience;
